export TF_VAR_test_key_pem="$(cat ~/.ssh/test-key.pem)"

ansible-playbook -i inventory.ini site.yml

ansible-playbook -i inventory.ini site_separated.yml --limit db

ansible-playbook -i inventory.ini site_separated.yml --limit web
